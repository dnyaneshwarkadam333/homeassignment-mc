Mastercard-home-assignment

For terraform initialisation:
#terraform init -conf-file=environment/developement/backend.config

For terraform plan:
#terraform plan -var-file=environment/developement/variable.tfvars

For terraform apply:
#terraform apply -var-file=environment/developement/variable.tfvars
